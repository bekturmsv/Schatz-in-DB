package pti.softwareentwicklg.SchatzInDb.model.enums;

public enum SqlKategorie {
    SELECT,
    WHERE,
    JOIN,
    GROUP_BY,
    HAVING,
    SUBQUERY,
    AGGREGATE,
    ORDER_BY,
    DISTINCT,
    COUNT,
    SUM
}

